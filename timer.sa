// timer.sa — QPM package
//
// Usage: from timer import starttimer, endtimer
//
//   from timer import starttimer, endtimer
//
//   starttimer()
//   ... your program ...
//   endtimer()
//
// Prints how long the code between the two calls took to run.

let _timer_start = 0;

export function starttimer() {
    _timer_start = time();
}

export function endtimer() {
    let _timer_end = time();
    let _elapsed = _timer_end - _timer_start;
    print("Execution time:", _elapsed, "seconds");
}
